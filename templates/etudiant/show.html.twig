{% extends 'base.html.twig' %}

{% block title %}Etudiant{% endblock %}

{% block stylesheets %}
    {{ parent() }}
 <!-- Include Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
{% endblock %}

{% block body %}
    <h1>Etudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ etudiant.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ etudiant.email }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ etudiant.age }}</td>
            </tr>
        </tbody>
    </table>
    
 <a href="{{ path('app_etudiant_index', {'id': etudiant.id}) }}" class="btn btn-danger">back to list</a>
 <a href="{{ path('app_etudiant_edit', {'id': etudiant.id}) }}" class="btn btn-danger">edit</a>

    {{ include('etudiant/_delete_form.html.twig') }}
{% endblock %}
