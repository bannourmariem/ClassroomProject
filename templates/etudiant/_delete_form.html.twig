

<!DOCTYPE html>
<html>
<head>
    <title>Your Page Title</title>
    <style>
     
.btn.btn-primary {
    background-color: #007bff;
    color: #fff; 
    border: none;
    padding: 
    cursor: pointer; 
}

.btn.btn-primary:hover {
    background-color: #0056b3; 
   
} 
    </style>
</head>
<body>

    <form method="post" action="{{ path('app_etudiant_delete', {'id': etudiant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudiant.id) }}">
        <button class="btn btn-primary">Delete</button>
    </form>
</body>
</html>